Generate a Nuclei template based on the following requirements:

## Vulnerability Information:
**Description**: {prompt}

## Similar Templates Found:
{retrieval_context}

## CRITICAL REQUIREMENTS:
1. Output ONLY valid YAML content - no explanations, no markdown formatting
2. Start with 'id:' as the first line
3. Follow exact Nuclei v3 YAML structure
4. Use proper YAML indentation (2 spaces)
5. All strings with special characters MUST be quoted
6. Use | or > for multiline strings when needed

## Template Structure (MANDATORY):
```
id: template-name
info:
  name: "Template Name"
  author: author-name
  severity: high|medium|low|info
  description: "Description here"
  tags: tag1,tag2,tag3
  classification:
    cvss-score: X.X
    cwe-id: CWE-XXX
requests:
  - method: GET|POST
    path:
      - "{{BaseURL}}/path"
    headers:
      Header-Name: "value"
    body: |
      body content here
    matchers-condition: and|or
    matchers:
      - type: word|status|regex
        words:
          - "string1"
          - "string2"
        condition: and|or
```

Generate ONLY the YAML template without any explanations or markdown blocks.